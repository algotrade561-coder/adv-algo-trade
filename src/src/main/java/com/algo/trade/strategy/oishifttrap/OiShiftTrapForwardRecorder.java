package com.algo.trade.strategy.oishifttrap;

import org.springframework.stereotype.Service;

/**
 * Phase 6 stub — original legacy forward-checkpoint recorder retired; the unified
 * {@code ForwardCheckpointService} now handles forward checkpoints across all
 * strategies. This stub keeps the legacy bean wiring intact so callers compile.
 */
@Service
public class OiShiftTrapForwardRecorder {

    public void registerSignal(Object... args) { /* no-op */ }

    public void record(Object... args) { /* no-op */ }
}
